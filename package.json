{
  "name": "pharmacychat",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^9.2.1"
  },
   "scripts": {
    "dev": "bun run index.ts",
    "build": "cd packages/client && bun run build && cd ../server && bun run tsc",
    "start": "node packages/server/index.ts"
  },
   "workspaces": [
    "packages/*"
  ],
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "type": "module"
}
